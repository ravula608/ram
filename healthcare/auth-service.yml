spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/capstone_inventory
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  
server:
  port: 9000

message: This is from Auth-service COnfig server
